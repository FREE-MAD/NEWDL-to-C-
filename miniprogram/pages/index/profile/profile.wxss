/* ============================================================
 * 教练资料编辑页 - 外壳样式
 * 完整 UI 样式全部内聚在 profile_coachbaseinformation 组件内。
 * 这里只设置页面底色，避免组件未完全撑满时露出默认白底，与组件 .page 保持一致。
 * ============================================================ */

page {
  background: #f5f6fa;
}
